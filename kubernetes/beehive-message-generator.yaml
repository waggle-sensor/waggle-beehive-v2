apiVersion: apps/v1
kind: Deployment
metadata:
  name: beehive-message-generator
spec:
  selector:
    matchLabels:
      app: beehive-message-generator
  template:
    metadata:
      labels:
        app: beehive-message-generator
    spec:
      containers:
      - name: beehive-message-generator
        image: waggle/beehive-message-generator
        env:
          - name: RABBITMQ_HOST
            value: "beehive-rabbitmq"
          - name: RABBITMQ_PORT
            value: "5671"
          - name: RABBITMQ_CACERTFILE
            value: "/etc/tls/cacert.pem"
          - name: RABBITMQ_CERTFILE
            value: "/etc/tls/cert.pem"
          - name: RABBITMQ_KEYFILE
            value: "/etc/tls/key.pem"
        volumeMounts:
          - name: tls
            mountPath: "/etc/tls"
            readOnly: true
      volumes:
        - name: tls
          secret:
            secretName: beehive-message-generator-tls-secret
