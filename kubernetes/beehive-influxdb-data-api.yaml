apiVersion: v1
kind: Service
metadata:
    name: beehive-influxdb-data-api
spec:
    ports:
      - name: http
        port: 10000
        targetPort: 10000
    selector:
        app: beehive-influxdb-data-api
    type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: beehive-influxdb-data-api
spec:
  selector:
    matchLabels:
      app: beehive-influxdb-data-api
  template:
    metadata:
      labels:
        app: beehive-influxdb-data-api
    spec:
      containers:
      - name: beehive-influxdb-data-api
        image: waggle/beehive-influxdb-data-api
        ports:
          - containerPort: 10000
            name: http
        env:
          - name: INFLUXDB_URL
            value: "http://beehive-influxdb:8086"
          - name: INFLUXDB_BUCKET
            value: "waggle"
          - name: INFLUXDB_TOKEN
            valueFrom:
              secretKeyRef:
                name: beehive-influxdb-data-api-secret
                key: token
